/**
 * React Editor Layout - Standalone
 * No admin wrapper, completely independent
 */

export default function ReactEditorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

