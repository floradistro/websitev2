/**
 * WCL Editor Layout - Standalone
 * No admin wrapper, completely independent
 */

export default function WCLEditorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

